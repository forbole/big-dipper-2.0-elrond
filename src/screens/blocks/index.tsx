import React from 'react';
import {
  Layout,
} from '@components';
import {
  List,
} from './components';

const Blocks = () => {
  return (
    <Layout>
      <List />
    </Layout>
  );
};

export default Blocks;
