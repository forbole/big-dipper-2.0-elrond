import React from 'react';
import {
  Box,
} from '@components';

const Details: React.FC<ComponentDefault> = (props) => {
  return (
    <Box className={props.className}>
      Details
    </Box>
  );
};

export default Details;
