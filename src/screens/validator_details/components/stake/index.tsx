import React from 'react';
import {
  Box,
} from '@components';

const Stake: React.FC<ComponentDefault> = (props) => {
  return (
    <Box className={props.className}>
      Stake
    </Box>
  );
};

export default Stake;
