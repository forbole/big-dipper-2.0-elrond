import React from 'react';
import {
  Box,
} from '@components';

const Profile: React.FC<ComponentDefault> = (props) => {
  return (
    <Box className={props.className}>
      Profile
    </Box>
  );
};

export default Profile;
