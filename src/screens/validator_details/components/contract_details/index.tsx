import React from 'react';
import {
  Box,
} from '@components';

const ContractDetails: React.FC<ComponentDefault> = (props) => {
  return (
    <Box className={props.className}>
      ContractDetails
    </Box>
  );
};

export default ContractDetails;
